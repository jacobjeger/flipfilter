<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="System Service"
        android:supportsRtl="true">

        <!-- Device Admin Receiver — this is the Device Owner component -->
        <receiver
            android:name=".AdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver — re-apply restrictions on every boot -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Maintenance receiver — toggle restrictions via ADB broadcast -->
        <receiver
            android:name=".MaintenanceReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.kosherflip.MAINTENANCE_MODE" />
            </intent-filter>
        </receiver>

        <!--
            No launcher activity — this app is completely hidden.
            It only runs as a Device Owner to enforce restrictions.
        -->

    </application>

</manifest>
